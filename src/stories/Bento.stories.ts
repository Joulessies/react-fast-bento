
import { Meta, Story } from '@storybook/react';
import Bento from './Bento';

export default {
    title: "Components/Bento",
    component: Bento,
} as Meta;

const Template: Story = (args) => <Bento>
